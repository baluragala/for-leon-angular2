import { UnpublishedDirective } from './unpublished.directive';

describe('UnpublishedDirective', () => {
  it('should create an instance', () => {
    const directive = new UnpublishedDirective();
    expect(directive).toBeTruthy();
  });
});
